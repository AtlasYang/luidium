import { HiOutlineDocumentDownload } from "react-icons/hi";

export default function DocumentDownloadIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <HiOutlineDocumentDownload size={size} color={color} />;
}
