import { FaTimesCircle } from "react-icons/fa";

export default function TimesCircleIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <FaTimesCircle size={size} color={color} />;
}
